# SUPPORT
Common issues:
- **Use PowerShell 7** (`pwsh`), not `cmd`.
- **Selection UI** needs `Out-GridView`; otherwise numeric picker is used.
- **Rate limits**: large private sets may hit API limits; re-run later.

Open issues: https://github.com/rickballard/gh-rz/issues
